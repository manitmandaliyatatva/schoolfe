export enum ListDisplayType {
  Default = 'none',
  Date = 'date',
  Currency = 'currency',
  Percentage = 'percentage',
  Checkbox = 'checkbox',
  Badge = 'badge',
  Link = 'link',
  Email = 'email',
  Phone = 'phone',
  Truncate = 'truncate',
  Progress = 'progress',
  Number = 'number',
}
